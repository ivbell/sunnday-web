import { Box } from '@chakra-ui/react'
import React, { FC } from 'react'

const AddTask: FC = () => {
  return (
    <Box zIndex={2} position={'absolute'} w={200} p={15} bgColor={'sn.bg_accent'}>
      123
    </Box>
  )
}

export default AddTask
