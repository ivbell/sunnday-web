import React, { FC } from 'react'
import MainLayout from '../components/layouts/MainLayout'

const TaskFolder: FC = () => {
  return <MainLayout>Task folder page</MainLayout>
}

export default TaskFolder
